# 足球比赛预测 FYP —— 项目知识库（全量总览）

> 建立日期：2026-08-31 ｜ 数据泄漏修复后的诚实版本
> 本文件是项目的**人类可读知识库**，独立于 Claude 的隐藏记忆存在。
> 数字权威来源：[docs/leakage_clean_review.md](docs/leakage_clean_review.md)（完整审查文档）；本文件是其精简+建模思路扩充版。

---

## 目录

1. [项目基线](#1-项目基线)
2. [建模思路详解](#2-建模思路详解)
3. [干净数字总览（泄漏修复后）](#3-干净数字总览)
4. [泄漏问题：机制与教训](#4-泄漏问题机制与教训)
5. [存活信号 vs 已作废主张](#5-存活信号-vs-已作废主张)
6. [复现命令](#6-复现命令)

---

## 1. 项目基线

| | Track A | Track B |
|:--|:--|:--|
| **范围** | 英四级 E0-E3（英超/英冠/英甲/英乙） | 英超单级别 |
| **赛季** | 7 季（2019/20–2025/26） | 6 季（2019/20–2024/25） |
| **场次** | 13,988（80/20 测试 2,798） | 2,280（80/20 测试 456） |
| **外部数据** | 无，纯特征工程 | FootyStats（PPG/xG/控球） |
| **特征数（修复后）** | 50 | 58 |

**FootyStats 付费墙**：免费数据只到 2024/25 赛季，所以 Track B 只能取 6 季。

**关键口径**：论文早期写"6 季 11,952 场"与最终约定"7 季 13,988 场"不一致，已统一为 7 季口径（`backtest_english4.py` 已移除 2025/26 排除行）。

---

## 2. 建模思路详解

### 2.1 核心思想：滚动窗口特征 + 只用手前信息

两个 Track 共用同一套特征工程管线 `build_features`（[league/src/features.py:490](league/src/features.py#L490)），scripts 里统一传 `windows=[5, 10]`。

**5/10 两个窗口的含义**：
- **5 场窗口**：抓短期状态（近 5 场状态，相当于"当前状态值"）
- **10 场窗口**：抓稳定水平（近 10 场趋势，抵抗单场偶然性）

**最重要的设计原则：所有滚动特征 `shift(1)` 滞后**——每场比赛的特征只包含**此前**的比赛，绝不包含当场（[features.py:199](league/src/features.py#L199)）。这是"无未来信息泄漏"的第一层保证。

### 2.2 特征分组

**① 滚动球队状态（28 个）** —— `_team_rolling_stats`（[features.py:163](league/src/features.py#L163)）

每个窗口 × 主/客视角算 7 个：

| 特征 | 含义 |
|:--|:--|
| `avg_goals_for_{5,10}` | 近 N 场场均进球 |
| `avg_goals_against_{5,10}` | 场均失球 |
| `avg_goal_diff_{5,10}` | 场均净胜球 |
| `avg_points_{5,10}` | 场均积分（胜 3 平 1 负 0） |
| `win_rate / draw_rate / loss_rate_{5,10}` | 近 N 场胜/平/负频率 |

→ 7 × 2 窗口 × 主客 2 = **28 个**

**② 滚动射门特征（16 个）** —— `_shot_features`（[features.py:253](league/src/features.py#L253)），只对前 2 个窗口算

| 特征 | 含义 |
|:--|:--|
| `avg_shots_{5,10}` | 场均射门 |
| `avg_sot_{5,10}` | 场均射正 |
| `shot_accuracy_{5,10}` | 射正率（SOT/射门） |
| `conversion_rate_{5,10}` | 进球转化率（进球/射门） |

→ 4 × 主客 2 × 2 窗口 = **16 个**

**③ 对战历史（5 个）** —— `_h2h_features`，固定窗口 5（近 5 次交手：主队进球/失球/胜率等）

**④ FootyStats 外部特征（9 个，仅 Track B）** —— `_footystats_extra_features`（[features.py:79](league/src/features.py#L79)）

| 特征 | 含义 |
|:--|:--|
| `home_ppg / away_ppg / ppg_diff` | 跨赛季场均积分（真实球队强度） |
| `pre_match_xg_home / away / diff` | 赛前预期进球 |
| `home_possession / away_possession / possession_diff` | 控球率 |

按"队名 + 日期"merge 到英超场次，主客对称。

### 2.3 模型与验证

- **模型**：Logistic Regression（L2 正则，lbfgs 求解器，C=1.0，max_iter=4000），三分类 H/D/A
- **特征数**：Track A = 28+16+5+1（league_level 联赛等级 0–3）= **50**；Track B = 28+16+5+9 = **58**
- **验证方式**：
  - **Walk-forward**（主评估）：逐年训练（用所有历史赛季）→ 测当年，依次滚动
  - **80/20 时序分割**（主评估）：前 80% 训练、后 20% 测试
  - **跨季 4/2**（Track B 附加实验）：train 2019-2022 / test 2023-24+2024-25（760 场整季 hold-out，因 FootyStats 免费数据到 24/25 而定）

### 2.4 价值投注框架

以模型概率对市场赔率算 **edge**：`edge = P_model / P_market − 1`，超过阈值（10% 或 30%）才下注。三种策略：Buchdahl 主胜流 / 全部 outcome / 每场最优单注。**市场赔率统一取收盘价（B365C）**，与准确率对比同一口径。

---

## 3. 干净数字总览

> ⚠️ 以下全部是**泄漏修复后**的诚实数字。论文/README 里旧的漂亮数字（58.60%、68.42%、67.11%、69.74%、70.18%）全部作废。

### 3.1 Track A（E0-E3，7 季 13,988 场，50 特征）

| 指标 | 干净 | 市场 | 差额 |
|:--|:--:|:--:|:--:|
| 80/20 准确率（2,798 测试） | **46.4%** | 49.3%（收盘） | −2.9pp |
| Walk-forward 准确率 | **45.1%** | 49.4%（收盘） | −4.3pp |
| 价值投注 ROI（best-per-match，edge>10%，收盘 B365C） | **−6.40%**（胜率 27.1% < 随机 1/3，0/6 季为正） | — | — |
| 树模型 LR / RF / XGB（7 季 WF） | **45.05 / 45.17 / 41.31** | — | — |

### 3.2 Track B（英超，6 季 2,280 场，58 特征）

| 指标 | 干净 | 市场 | 差额 |
|:--|:--:|:--:|:--:|
| **Walk-forward（全季，1,900 测试，主评估）** | **50.21%** | 56.32%（收盘） | **−6.11pp**（p<0.001 显著输） |
| 80/20 准确率（456 测试，全 58 特征管线，主评估） | **54.61%** | 57.24%（收盘） | −2.63pp |
| 跨季 4/2（760 测试，全管线）〔附加〕 | **53.29%** | 57.63%（收盘） | −4.34pp |
| 跨季 4/2（**精简 25 特征** fair_comparison）〔附加〕 | **59.08%** | 57.63%（收盘） | **+1.45pp**（不显著） |
| 跨季 4/2（footystats_backtest 滚动+赛前）〔附加〕 | **58.55%** | 57.63%（收盘） | **+0.92pp**（不显著） |
| Buchdahl 主胜 ROI（edge>10%，收盘 B365C） | **+3.69%** | — | — |
| 全部 outcome ROI（收盘 B365C） | **+2.12%** | — | — |
| 最优单注 ROI（收盘 B365C） | **+11.95%** | — | — |

### 3.3 其他实验（全部干净版）

| 实验 | 干净结果 | 说明 |
|:--|:--|:--|
| DL：LR / MLP-Deep 准确率 | **61.4% / 61.4%** | 与泄漏版 70.18% 差很多 |
| DL：MLP-Deep 平局 F1 | **0.000**（LR 0.036） | 原"DrawF1 +85%"主张作废 |
| Two-tier：Tier1（有 FS） | **61.84%**（58 特征，DrawF1 0.097） | 原 69.74% 作废 |
| Two-tier：Tier2（无 FS fallback） | **48.58%**（51 特征） | 原 60.26% 作废 |
| 2025-26 回测 vs 市场 | **+1.31pp**（46.05% vs 44.74% 收盘） | 原 +14.73pp 作废 |
| ~~O/U 2.5 准确率~~ | 55.48%（AUC 0.5302） | **已剔除，不纳入论文** |

### 3.4 核心洞察：统一收盘基线后，模型不跑赢市场

- **市场基线统一为收盘赔率**（Bet365 收盘线 B365C，开赛前的市场共识）。全脚本同一口径，不再分"开盘/收盘"两套基准。
- **主评估 = 全季 walk-forward + 80/20，两条线都输**：全季 walk-forward（58 特征，6 季逐年滚动，1,900 测试）模型 **50.21%** vs 收盘 **56.32%**，**−6.11pp，McNemar p<0.001 显著跑输**；80/20 全管线 54.61% vs 57.24%（−2.6pp）。
- **唯一"接近"的是附加实验 4/2 上的精简模型**：fair_comparison 59.08% vs 57.63%（+1.45pp）、footystats_backtest 58.55%（+0.92pp）——760 场、不显著、且非主评估测试集。按"不显著=输"仍判未跑赢。
- **论文原口径问题**：早期 Track A 用收盘价、Track B 用开盘价，两套口径不一致，曾造成"赢开盘"的假象。

---

## 4. 泄漏问题：机制与教训

### 4.1 一句话

`get_feature_columns` 把 12 个**当场统计列**（射门/射正/犯规/角球/黄牌/红牌 × 主客）直接当特征透传、没滞后——用**比赛自身的结果指标**预测比赛本身，教科书级 target leakage。

### 4.2 完整机制（9 步）

1. football-data.co.uk 原始 CSV 每场带当场统计（射门/射正/角球/犯规/黄红牌×主客），这些是**赛后才知道**的数字，本质是比赛结果的"部分答案"。
2. `data_loader.py` 的 COLUMN_MAP 把它们标准化为 `home_shots` 等 12 个字段。
3. 特征工程**设计意图是只用赛前信息**——所有滚动特征 `shift(1)`（只含此前 5/10 场均值，[features.py:199-286](league/src/features.py#L199)）。论文"无未来信息泄漏"原指这一层。
4. 但 `build_features` 末尾 `result = df.copy()`（[features.py:522](league/src/features.py#L522)）把整个原始 DataFrame **原样透传**，当场统计列跟着进了特征矩阵。
5. `get_feature_columns` 用 **黑名单** 过滤，黑名单只排除了"结果类"列（比分/赛果/总进球/胜平负），**漏了 12 个"统计线"列**——它们同样是结果的部分答案。
6. 于是 LR 输入同时含：✅滞后滚动特征（赛前）+ ❌本场自己的射门/角球/犯规/牌（赛后）。
7. 当场统计对胜负**近乎确定性**（射门 20 vs 5 几乎必赢）→ 泄漏版 Track A 58.60% / Track B 68.42%，移除后掉到 **46.4% / 54.61%**，低于市场。
8. **一个口子传染全部**：12 个脚本共享同一个 `get_feature_columns`（DL/ensemble/O/U/树/价值投注全走它），所以 69.74%、70.18%、67.11% 同根。
9. 修复选择 **"移除"而非"滞后"**：A/B 实验滞后版 47.05% < 移除版 47.55%，且滚动射门特征本已是滞后信息，移除更干净。

### 4.3 教训

- 特征工程的"干净"必须落在**最终特征矩阵**上，而不是设计意图上。
- 黑名单（denylist）过滤天生不安全——会漏列。白名单（只放构造的滚动特征）才是防泄漏的正确姿势。
- 一个公共特征入口被污染，所有下游实验一起遭殃。

---

## 5. 存活信号 vs 已作废主张

### 仍然站得住的发现

1. **模型不跑赢收盘市场（严格二分）**：主评估（全季 walk-forward 50.21% vs 56.32% **显著输**；80/20 54.61% vs 57.24% 输）下模型全部跑输或持平；附加 4/2 精简模型 +1.45pp/+0.92pp 但不显著。按"不显著=输"，模型未跑赢市场。
2. **FootyStats 增量真实**：fair_comparison 滚动 53.55% → +FootyStats 59.08%（**+5.5pp**）。
3. **Buchdahl 价值投注 ROI 仍正（收盘价 B365C）**：干净模型下主胜 +3.69%、全部 +2.12%、最优单注 +11.95%（edge>10%）。注：模型整体准确率低于市场却 ROI 为正，利润来自特定方向的系统性偏差，需统计检验后再入论文。
4. **模型选择结论不变**：LR ≥ 树模型 ≥ DL。
5. **泄漏量级本身是发现**：当场统计列抬高了 10~14pp 准确率——足球预测 ML 里一个有分量的方法论警示。

### 已作废 / 不可复现的论文数字

- Track B 68.42% / 基线 62.28%
- Buchdahl ROI +5.41% / +9.77% / +22.71%
- Track A 80/20 +62.8% ROI、WF +52~56% ROI
- O/U 67.11%、ensemble 69.74%/60.26%、DL 70.18%/DrawF1 0.381

**原则**：以上全部以"当前脚本复跑输出"为准，见 [复现命令](#6-复现命令)。

---

## 6. 复现命令

```powershell
# Track A：7 季 walk-forward + 价值投注 + 阈值扫描（50 特征）
python scripts/backtest_english4.py

# Track A：三策略价值投注（已修复 bug，含二项检验）
python scripts/value_betting_walkforward.py

# Track A：树模型对比 LR/RF/XGB（7 季 WF）
python scripts/tree_comparison.py

# Track B：全季 walk-forward + 每季收盘市场基线 + Buchdahl ROI（58 特征，投注赔率=收盘 B365C，summary.json 含 walkforward_vs_market）
python scripts/buchdahl_ppg_walkforward.py

# Track B：fair_comparison（25 特征，4/2 分割，59.08% vs 收盘市场 57.63%）
python scripts/fair_comparison.py

# Track B：footystats_backtest（滚动+阵容+赛前，58.55% vs 收盘市场 57.63%）
python scripts/footystats_backtest.py

# Track B：80/20 + 跨季 4/2 干净评估（58 特征全管线）
python scripts/trackb_clean_eval.py

# Track B：O/U 2.5（已剔除，不纳入论文）
python scripts/over_under_experiment.py

# Two-tier ensemble（Tier1 61.84% / Tier2 48.58% / 2025-26 +0.52pp）
python scripts/build_ensemble_model.py

# DL（LR/MLP/GRU 对比，58 特征）
python scripts/deep_learning_experiment.py
```

**关键脚本产出存档**：
- `outputs/buchdahl_walkforward/summary.json` —— 干净 Buchdahl ROI
- `outputs/value_betting/summary.json` —— 干净 Track A 价值投注
- `outputs/ensemble/results.json` —— 干净 ensemble
- `outputs/deep_learning/` —— 干净 DL
- `outputs/footystats_backtest/` —— 干净 footystats_backtest

---

*维护提示：本文件会随项目进展更新；数字变更请同步核对 [docs/leakage_clean_review.md](docs/leakage_clean_review.md)。*
